{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the MarkdownEditor window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
